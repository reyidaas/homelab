name: cloudflared
services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel run homelab
    volumes:
      - /opt/homelab/.cloudflared/cert.pem:/etc/cloudflared/cert.pem:ro
      - /opt/homelab/.cloudflared/config.yml:/etc/cloudflared/config.yml:ro
      - /opt/homelab/.cloudflared/${TUNNEL_ID}.json:/etc/cloudflared/${TUNNEL_ID}.json:ro
    networks:
      - proxy
networks:
  proxy:
    external: true
